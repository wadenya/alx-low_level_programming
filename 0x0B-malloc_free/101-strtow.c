#include "main.h"
#include <stdio.h>

void util(char **, char *);
void create_word(char **, char *, int, int, int);

/**
 * strtow - splits a string into words.
 * @str: a string
 *
 * Return: void
 */
char **strtow(char *str)
{
	int y, g, h;
	char **d;

	if (tr == NULL || str[0] == '\0' || (str[0] == ' ' && str[1] == '\0'))
		return (NULL);

	y = g = h = 0;
	while (str[y])
	{
	}
}
